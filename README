#DESAFIO 5 - Api Node + Express

1-Para iniciar, va até  a pasta  Api/src  e execute o comando:

`npm install` para installar as dependencias 

2-Execute o comando:

`npm run dev`  para iniciar o servidor 

o servidor iniciara e uma mensagem com  a porta sera exibida.